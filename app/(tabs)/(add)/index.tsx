import React from 'react'
import { ScrollView, View, Text } from 'react-native'

export default function add() {
  return (
    <ScrollView>
        <View>
            <Text>Add</Text>
        </View>
    </ScrollView>
  )
}
